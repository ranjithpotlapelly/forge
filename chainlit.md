# Forge

A self-hosted repo copilot. Ask a question about the indexed codebase and get
an answer with file+line citations, retrieved from the local Chroma index and
answered by a local Ollama model — nothing leaves this machine.

Traces for every request are in Phoenix: http://localhost:6006
